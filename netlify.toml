# Netlify Configuration
[build]
  publish = "."

# Environment variables (set in Netlify dashboard, not in this file)
# FORMSUBMIT_ID=your-activation-id-here
# FORMSUBMIT_EMAIL=recieving-email

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Note: Axon installer script is hosted in the mlOS-foundation/axon repository
# Cloudflare redirect rule configured: axon.mlosfoundation.org -> GitHub raw content
# See CLOUDFLARE_SETUP.md for configuration details

